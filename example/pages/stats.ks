fn build() {
  return Scaffold({
    appBar: AppBar({ title: "Statistiques", backgroundColor: Theme.primary }),
    body: Center(
      Text("Bientôt disponible !", { fontSize: 20, color: "grey" })
    )
  })
}
